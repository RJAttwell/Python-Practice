#Tuples are similar to lists but with a key difference

coordinates = (4, 5) #Tuple 
#coordinates = [(4,5), (6,7), (8,9)] #Can create a list of tuples

#coordinates[1] = 10 
#An error would occur here as you cannot change the data inside a tuple

print(coordinates[0])
